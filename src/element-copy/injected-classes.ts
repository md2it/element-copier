import { HIGHLIGHT_UI } from "../pick-mode/page-styles";

/** Page classes injected by pick-mode highlight (strip before extract). */
export const ELEMENT_COPY_INJECTED_CLASS_NAMES = [
  HIGHLIGHT_UI.highlightTarget,
  HIGHLIGHT_UI.highlightFill,
] as const;
