{
  "name": "element-copier",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "icons:manifest": "node src/lib/scripts/generate-manifest-icons.mjs",
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "dist": "npm run build && node scripts/package-extension.mjs",
    "typecheck": "node ../lib/node_modules/typescript/bin/tsc --noEmit -p tsconfig.json"
  }
}
